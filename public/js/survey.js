const $ = function (selector) {

}

//converting each user's results into a simple array of numbers, and compare the difference between current user's score against those from other users.
//Use absolute value of the differences